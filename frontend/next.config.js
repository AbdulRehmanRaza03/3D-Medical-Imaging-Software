/** @type {import('next').NextConfig} */
const nextConfig = {
  reactStrictMode: true,
  // API base URL is injected at build time via NEXT_PUBLIC_* env vars.
  env: {},
};

module.exports = nextConfig;
